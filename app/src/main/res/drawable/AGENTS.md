# Drawable 资源指南

> 位置：`app/src/main/res/drawable/`

本目录包含 `tv_banner.xml` 和旧版矢量 `ic_launcher.xml`。Manifest 当前使用 mipmap PNG 作为应用图标，`tv_banner.xml` 仍用于 Leanback 横幅。修改横幅需检查 320×180 比例和电视桌面裁切；不要把 QA 截图放入此目录。

